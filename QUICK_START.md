# 🚀 دليل البدء السريع

## خطوات التشغيل

### الخطوة 1️⃣: تحديث عنوان الخادم

افتح الملف: `lib/config/api_config.dart`

غيّر هذا السطر:
```dart
static const String baseUrl = 'http://localhost:8080/api/v1';
```

إلى عنوان خادمك الفعلي.

**أمثلة:**
- للهاتف الحقيقي: `http://192.168.1.100:8080/api/v1`
- لمحاكي Android: `http://10.0.2.2:8080/api/v1`
- للتطوير المحلي: `http://localhost:8080/api/v1`

---

### الخطوة 2️⃣: تحديث الحزم

```bash
cd /home/dexter/flutter_apps/stronger_muscles_dashboard
flutter pub get
```

---

### الخطوة 3️⃣: تشغيل التطبيق

```bash
flutter run
```

---

## ✅ ما سيحدث

1. **عند الفتح:**
   - التطبيق سيتحقق من الاتصال بالخادم
   - سيجلب البيانات من API

2. **إذا كان الخادم متوفراً:**
   - ✅ سيعرض جميع البيانات من قاعدة البيانات
   - ✅ سيظهر "متصل" في الأعلى (نقطة خضراء)

3. **إذا كان الخادم غير متوفر:**
   - ❌ سيعرض شاشة خطأ
   - ❌ سيظهر "غير متصل" في الأعلى (نقطة حمراء)
   - 🔄 يمكنك الضغط على "إعادة محاولة"

---

## 🧪 اختبار الاتصال

### تشغيل خادم تجريبي:

```bash
# مثال باستخدام Node.js
npm install express
node server.js
```

أو استخدم أي خادم آخر يعمل على المنفذ 8080.

---

## 📊 البيانات المتوقعة من API

يجب أن يكون الخادم يدعم هذه الـ Endpoints:

| Endpoint | الوصف |
|----------|--------|
| `GET /api/v1/health` | التحقق من حالة الخادم |
| `GET /api/v1/orders` | قائمة الطلبات |
| `GET /api/v1/products` | قائمة المنتجات |
| `GET /api/v1/categories` | قائمة التصنيفات |

---

## 🎯 ملخص سريع

| العنصر | التفاصيل |
|--------|---------|
| 🔧 الإعدادات | `lib/config/api_config.dart` |
| 📡 الخدمات | `lib/services/api_service.dart` |
| 🎮 المتحكم | `lib/controllers/dashboard_controller.dart` |
| 🖼️ الشاشة | `lib/screens/dashboard_screen.dart` |
| 📚 الدليل الكامل | `DATABASE_CONNECTION.md` |

---

## ❓ الأسئلة الشائعة

**س: أين أعدّل عنوان الخادم؟**
ج: في `lib/config/api_config.dart` على السطر `baseUrl`

**س: كيف أعيد محاولة الاتصال؟**
ج: اضغط زر "إعادة محاولة" أو أعد تحميل الشاشة

**س: ماذا إذا كان الخادم يعطي خطأ؟**
ج: راجع `DATABASE_CONNECTION.md` لاستكشاف الأخطاء

**س: هل يمكن عرض بيانات وهمية؟**
ج: لا، التطبيق الآن يعرض فقط بيانات من API

---

## 🚀 جاهز للبدء!

اتبع الخطوات الثلاث أعلاه وستكون جاهزاً للتشغيل.

**تم التحديث:** يناير 2026
