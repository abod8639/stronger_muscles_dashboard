# 🔌 دليل الاتصال بقاعدة البيانات

## 📌 نظرة عامة

تم تحديث التطبيق ليعرض بيانات حقيقية من قاعدة البيانات فقط مع التحقق من حالة الاتصال والتعامل مع الأخطاء بشكل احترافي.

---

## 🏗️ الهيكل المعماري

### طبقات البيانات:

```
┌─────────────────────────────────────┐
│   UI Layer (Screens & Components)   │
└──────────────┬──────────────────────┘
               │
┌──────────────┴──────────────────────┐
│   Controller Layer (GetX)            │
│   (DashboardController)              │
└──────────────┬──────────────────────┘
               │
┌──────────────┴──────────────────────┐
│   Repository Pattern                 │
│   (OrderRepository, etc.)            │
└──────────────┬──────────────────────┘
               │
┌──────────────┴──────────────────────┐
│   Service Layer (ApiService)        │
│   (HTTP Requests)                    │
└──────────────┬──────────────────────┘
               │
        ┌──────┴───────┐
        │   HTTP       │
        │   Network    │
        └──────────────┘
```

---

## 📁 الملفات الجديدة/المحدثة

### 1. **lib/services/api_service.dart**
خدمة التواصل مع API

**الدوال الرئيسية:**
```dart
checkConnection()          // التحقق من الاتصال بالخادم
fetchOrders()             // جلب جميع الطلبات
fetchOrderDetail(id)      // جلب تفاصيل طلب
fetchProducts()           // جلب جميع المنتجات
fetchCategories()         // جلب جميع التصنيفات
fetchUserProfile(token)   // جلب ملف المستخدم
fetchDashboardStats()     // جلب إحصائيات لوحة التحكم
```

### 2. **lib/repositories/**
معايير التخزين (Repository Pattern)

**الملفات:**
- `order_repository.dart` - إدارة بيانات الطلبات
- `product_repository.dart` - إدارة بيانات المنتجات
- `category_repository.dart` - إدارة بيانات التصنيفات

### 3. **lib/controllers/dashboard_controller.dart**
تحديث كامل للمتحكم

**الميزات الجديدة:**
- التحقق من الاتصال بقاعدة البيانات
- جلب البيانات من API الحقيقي
- معالجة الأخطاء الشاملة
- إعادة محاولة الاتصال
- رسائل خطأ واضحة

### 4. **lib/components/connection_status.dart**
مكونات جديدة لعرض حالة الاتصال

**المكونات:**
- `ConnectionStatusBar` - شريط حالة الاتصال العلوي
- `ErrorScreen` - شاشة خطأ شاملة
- `NoDataScreen` - شاشة عدم وجود بيانات

### 5. **lib/screens/dashboard_screen.dart**
تحديث شاشة لوحة التحكم

**التحسينات:**
- عرض شاشة خطأ عند فشل الاتصال
- عرض شاشة البيانات الفارغة عند عدم وجود بيانات
- شريط حالة الاتصال العلوي
- أيقونة حالة الاتصال الديناميكية في AppBar

---

## 🔄 سير العملية

### عند تشغيل التطبيق:

1. **التهيئة** (`onInit` في DashboardController)
   - إنشاء instances من الخدمات والمستودعات

2. **التحقق من الاتصال** (`checkConnection`)
   - إرسال طلب HTTP إلى `GET /api/v1/health`
   - تعيين حالة الاتصال (`isConnected`)

3. **جلب البيانات** (`fetchDashboardData`)
   - إذا كان الاتصال متاحاً:
     - جلب الطلبات
     - جلب المنتجات
     - جلب التصنيفات
   - حساب الإحصائيات

4. **عرض النتائج**
   - إذا كان الاتصال نجح: عرض البيانات
   - إذا فشل الاتصال: عرض شاشة خطأ
   - إذا لم تكن هناك بيانات: عرض "لا توجد بيانات"

---

## 🚨 معالجة الأخطاء

### أنواع الأخطاء:

#### 1. **خطأ الاتصال (Connection Error)**
```
السبب: الخادم غير متاح أو الإنترنت مقطوع
العرض: شاشة خطأ حمراء مع زر "إعادة محاولة"
الرسالة: "لا يمكن الاتصال بقاعدة البيانات"
```

#### 2. **مهلة الاتصال (Timeout)**
```
السبب: استغرق الطلب أكثر من 10 ثوان
العرض: شاشة خطأ مع تفاصيل
الرسالة: "انتهت مهلة الاتصال - تأكد من سرعة الإنترنت"
```

#### 3. **عدم وجود بيانات (No Data)**
```
السبب: قاعدة البيانات فارغة
العرض: شاشة رمادية بسيطة
الرسالة: "لا توجد بيانات متاحة"
```

#### 4. **خطأ التحليل (Parse Error)**
```
السبب: صيغة البيانات غير صحيحة
العرض: شريط تحذير أحمر في الأعلى
الرسالة: تفاصيل الخطأ التقني
```

---

## 📡 اعدادات API

### الملف: `lib/config/api_config.dart`

```dart
class ApiConfig {
  static const String baseUrl = 'http://localhost:8080/api/v1';
  
  // Endpoints
  static const String products = '/products';
  static const String categories = '/categories';
  static const String orders = '/orders';
  static const String userProfile = '/user';
}
```

### التعديلات المطلوبة:

**للهاتف الحقيقي:**
```dart
static const String baseUrl = 'http://YOUR_IP:8080/api/v1';
```

**لمحاكي Android:**
```dart
static const String baseUrl = 'http://10.0.2.2:8080/api/v1';
```

**لمحاكي iOS:**
```dart
static const String baseUrl = 'http://localhost:8080/api/v1';
```

---

## 🎯 حالات الاستخدام

### 1. **جلب البيانات الأولية:**
```dart
final controller = Get.find<DashboardController>();
// يتم الاتصال تلقائياً عند الإنشاء
```

### 2. **إعادة محاولة الاتصال يدويّاً:**
```dart
controller.retryConnection();
```

### 3. **تغيير الفترة الزمنية:**
```dart
controller.changePeriod('month');
// يعيد جلب البيانات مع الفترة الجديدة
```

### 4. **التحقق من حالة الاتصال:**
```dart
controller.isConnected.obs  // true/false
controller.errorMessage.obs // رسالة الخطأ إن وجدت
```

---

## 🧪 اختبار الاتصال

### اختبار محلي:

```bash
# تشغيل الخادم على المنفذ 8080
cd path/to/api-server
npm start  # أو الأمر الخاص بخادمك

# تشغيل التطبيق
cd /home/dexter/flutter_apps/stronger_muscles_dashboard
flutter run
```

### اختبار في المحاكي:

```bash
# إذا كان الخادم يعمل على localhost
# استخدم IP: 10.0.2.2 بدلاً من localhost
```

---

## 📊 مثال على استجابة API

### GET /api/v1/orders
```json
[
  {
    "id": "ORD-001",
    "user_id": "USR-001",
    "order_date": "2024-01-01T10:00:00",
    "status": "delivered",
    "payment_status": "paid",
    "payment_method": "card",
    "address_id": "ADDR-001",
    "subtotal": 250.00,
    "shipping_cost": 25.00,
    "discount": 0.0,
    "total_amount": 275.00
  }
]
```

---

## ✅ قائمة التحقق

- [x] إزالة البيانات الوهمية
- [x] إنشاء ApiService
- [x] إنشاء Repository Pattern
- [x] التحقق من الاتصال
- [x] معالجة الأخطاء الشاملة
- [x] عرض رسائل خطأ واضحة
- [x] إعادة محاولة الاتصال
- [x] عرض حالة الاتصال الديناميكية
- [x] شاشات الأخطاء المخصصة
- [x] دعم الفترات الزمنية

---

## 🚀 الخطوات التالية

1. **اختبر الاتصال** بالخادم الفعلي
2. **تحقق من استجابات API** مع نموذجك
3. **أضف endpoints إضافية** إذا لزم الأمر
4. **حسّن معالجة الأخطاء** حسب احتياجاتك
5. **أضف caching** للبيانات

---

## 🔗 الملفات ذات الصلة

- [API Service](../services/api_service.dart)
- [Dashboard Controller](../controllers/dashboard_controller.dart)
- [Order Repository](../repositories/order_repository.dart)
- [API Config](../config/api_config.dart)
- [Dashboard Screen](./dashboard_screen.dart)
- [Connection Status Component](../components/connection_status.dart)

---

**تم التحديث:** يناير 2026
**الحالة:** جاهز للاستخدام 🎉
