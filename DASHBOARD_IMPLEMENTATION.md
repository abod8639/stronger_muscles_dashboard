# 📊 لوحة التحكم - دليل التطوير

## 📌 نظرة عامة

تم تصميم لوحة تحكم شاملة متكاملة لمتجر Stronger Muscles وفقاً للنماذج المتعرّفة. اللوحة توفر:

- ✅ عرض الإحصائيات الرئيسية (الإيرادات، الطلبات، المستخدمين، المنتجات)
- ✅ الطلبات الأخيرة مع حالاتها
- ✅ توزيع حالات الطلبات (معلقة، تحت المعالجة، مرسلة، مُسلّمة، ملغاة)
- ✅ عرض التصنيفات
- ✅ حالة المخزون (متوفر، منخفض، غير متوفر)
- ✅ دعم الفترات الزمنية (أسبوع، شهر، سنة)

---

## 🏗️ هيكل المشروع

```
lib/
├── main.dart                          # نقطة الدخول الرئيسية
├── screens/
│   ├── dashboard_screen.dart         # شاشة لوحة التحكم
│   └── index.dart                     # تصدير الشاشات
├── controllers/
│   ├── dashboard_controller.dart     # متحكم لوحة التحكم
│   └── index.dart                     # تصدير المتحكمات
├── components/
│   ├── dashboard_stat_card.dart      # بطاقة الإحصائيات
│   ├── recent_orders_list.dart       # قائمة الطلبات الأخيرة
│   ├── categories_grid.dart          # شبكة التصنيفات
│   ├── order_status_chart.dart       # رسم بياني لحالات الطلبات
│   └── index.dart                     # تصدير المكونات
├── models/
│   ├── user.dart                      # نموذج المستخدم
│   ├── product.dart                   # نموذج المنتج
│   ├── order.dart                     # نموذج الطلب
│   ├── category.dart                  # نموذج التصنيف
│   ├── cart_item.dart                # نموذج عنصر السلة
│   └── index.dart                     # تصدير النماذج
└── config/
    ├── theme.dart                     # إعدادات المظهر
    └── api_config.dart               # إعدادات API
```

---

## 🎨 المكونات الرئيسية

### 1. **DashboardScreen** - شاشة لوحة التحكم
الشاشة الرئيسية التي تعرض جميع الإحصائيات والبيانات.

**الميزات:**
- AppBar مخصص مع مؤشر الاتصال
- محدد الفترات الزمنية (أسبوع، شهر، سنة)
- بطاقات إحصائيات قابلة للنقر
- قائمة الطلبات الأخيرة
- رسوم بيانية لحالات الطلبات والمخزون

```dart
DashboardScreen()
```

### 2. **DashboardStatCard** - بطاقة الإحصائيات
بطاقة تعرض إحصائية واحدة مع أيقونة ومؤشرات الاتجاه.

**الخصائص:**
- `title` - عنوان الإحصائية
- `value` - القيمة المراد عرضها
- `icon` - أيقونة مخصصة
- `color` - لون البطاقة
- `showTrendIcon` - عرض مؤشر الاتجاه
- `isTrendPositive` - اتجاه موجب أم سالب

```dart
DashboardStatCard(
  title: 'إجمالي الإيرادات',
  value: '45,250 ر.س',
  icon: Icons.trending_up,
  color: AppColors.primary,
  showTrendIcon: true,
  isTrendPositive: true,
)
```

### 3. **RecentOrdersList** - قائمة الطلبات الأخيرة
تعرض قائمة بأحدث الطلبات مع حالاتها والتواريخ.

**الخصائص:**
- `orders` - قائمة الطلبات
- `onSeeAll` - دالة عند الضغط على "عرض الكل"

```dart
RecentOrdersList(
  orders: controller.orders.take(3).toList(),
  onSeeAll: () {
    Get.toNamed('/orders');
  },
)
```

### 4. **OrderStatusChart** - رسم بياني لحالات الطلبات
يعرض توزيع الطلبات حسب الحالة مع نسب مئوية.

**الحالات:**
- `pending` - معلقة
- `processing` - تحت المعالجة
- `shipped` - مرسلة
- `delivered` - تم التسليم
- `cancelled` - ملغاة

```dart
OrderStatusChart(
  pending: 5,
  processing: 8,
  shipped: 12,
  delivered: 98,
  cancelled: 3,
)
```

### 5. **StockStatusChart** - حالة المخزون
يعرض حالة المنتجات في المخزون.

**الحالات:**
- `inStock` - متوفرة
- `lowStock` - مخزون منخفض
- `outOfStock` - غير متوفرة

```dart
StockStatusChart(
  inStock: 45,
  lowStock: 12,
  outOfStock: 5,
)
```

### 6. **CategoriesGrid** - شبكة التصنيفات
عرض التصنيفات في شبكة جميلة مع الصور.

**الخصائص:**
- `categories` - قائمة التصنيفات
- `onCategoryTap` - دالة عند اختيار تصنيف
- `onSeeAll` - دالة عند الضغط على "عرض الكل"

```dart
CategoriesGrid(
  categories: controller.categories,
  onCategoryTap: (category) {
    // معالجة اختيار التصنيف
  },
)
```

---

## 🎮 DashboardController

متحكم الحالة الرئيسي الذي يدير جميع البيانات.

### الخصائص الأساسية:

```dart
// الحالات
isLoading           // حالة التحميل
selectedPeriod      // الفترة المختارة (week, month, year)

// الإحصائيات
totalRevenue        // إجمالي الإيرادات
totalOrders         // عدد الطلبات
totalUsers          // عدد المستخدمين
totalProducts       // عدد المنتجات

// قوائم البيانات
orders              // قائمة الطلبات
products            // قائمة المنتجات
users               // قائمة المستخدمين
categories          // قائمة التصنيفات

// إحصائيات الطلبات
pendingOrders       // عدد الطلبات المعلقة
processingOrders    // عدد الطلبات تحت المعالجة
shippedOrders       // عدد الطلبات المرسلة
deliveredOrders     // عدد الطلبات المُسلّمة
cancelledOrders     // عدد الطلبات الملغاة

// إحصائيات المنتجات
productsInStock     // عدد المنتجات المتوفرة
productsLowStock    // عدد المنتجات ذات المخزون المنخفض
productsOutOfStock  // عدد المنتجات غير المتوفرة
```

### الدوال الرئيسية:

```dart
// جلب البيانات
fetchDashboardData()

// تغيير الفترة الزمنية
changePeriod(String period)
```

---

## 🎨 نظام الألوان

الألوان المستخدمة معرّفة في `config/theme.dart`:

| الاسم | الكود | الاستخدام |
|-------|-------|---------|
| `primary` | #1F77D4 | اللون الأساسي (أزرق) |
| `secondary` | #FF6B6B | اللون الثانوي (أحمر) |
| `success` | #2ECC71 | حالة النجاح (أخضر) |
| `warning` | #FFA500 | تحذيرات (برتقالي) |
| `danger` | #E74C3C | أخطاء حرجة (أحمر غامق) |
| `pending` | #FFB74D | طلب معلق |
| `processing` | #42A5F5 | قيد المعالجة |
| `shipped` | #66BB6A | مرسل |
| `delivered` | #26C6DA | مُسلّم |
| `cancelled` | #EF5350 | ملغى |
| `inStock` | #4CAF50 | متوفر |
| `lowStock` | #FF9800 | مخزون منخفض |
| `outOfStock` | #F44336 | غير متوفر |

---

## 📱 الملاحة السفلية

يتم التنقل بين الشاشات المختلفة عبر BottomNavigationBar:

- 🏠 **لوحة التحكم** - DashboardScreen
- 🛒 **الطلبات** - (قريباً)
- 📦 **المنتجات** - (قريباً)
- 👥 **المستخدمون** - (قريباً)
- ⚙️ **الإعدادات** - (قريباً)

---

## 🚀 كيفية الاستخدام

### عرض لوحة التحكم:

```dart
import 'package:flutter/material.dart';
import 'screens/dashboard_screen.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: const DashboardScreen(),
    );
  }
}
```

### الحصول على بيانات محدثة:

```dart
final controller = Get.find<DashboardController>();
controller.fetchDashboardData();
```

### التصرف عند تغيير الفترة:

```dart
controller.changePeriod('month');
```

---

## 📊 نماذج البيانات

### OrderModel
- `id` - معرف الطلب
- `userId` - معرف المستخدم
- `orderDate` - تاريخ الطلب
- `status` - حالة الطلب
- `paymentStatus` - حالة الدفع
- `totalAmount` - المبلغ الإجمالي

### ProductModel
- `id` - معرف المنتج
- `name` - اسم المنتج
- `price` - السعر
- `stockQuantity` - كمية المخزون
- `averageRating` - متوسط التقييم

### CategoryModel
- `id` - معرف التصنيف
- `name` - اسم التصنيف
- `imageUrl` - رابط الصورة
- `isActive` - هل التصنيف نشط

### UserModel
- `id` - معرف المستخدم
- `name` - اسم المستخدم
- `email` - البريد الإلكتروني
- `phoneNumber` - رقم الهاتف

---

## 🔧 التخصيص والتوسع

### إضافة إحصائية جديدة:

```dart
// 1. أضف متغير جديد إلى DashboardController
final newStat = 0.obs;

// 2. استخدم DashboardStatCard في الواجهة
DashboardStatCard(
  title: 'إحصائية جديدة',
  value: controller.newStat.value.toString(),
  icon: Icons.your_icon,
  color: AppColors.yourColor,
)
```

### ربط API الفعلي:

```dart
// في DashboardController
Future<void> fetchDashboardData() async {
  try {
    isLoading.value = true;
    
    // استدعاء API
    final response = await http.get(
      Uri.parse('${ApiConfig.baseUrl}/dashboard-stats'),
    );
    
    if (response.statusCode == 200) {
      // معالجة البيانات
      // تحديث المتغيرات الحالية
    }
    
    isLoading.value = false;
  } catch (e) {
    Get.snackbar('خطأ', 'فشل في جلب البيانات');
    isLoading.value = false;
  }
}
```

---

## ✅ قائمة المميزات المنفذة

- [x] عرض الإحصائيات الرئيسية
- [x] الطلبات الأخيرة
- [x] توزيع حالات الطلبات
- [x] عرض التصنيفات
- [x] حالة المخزون
- [x] محدد الفترات الزمنية
- [x] مؤشرات الاتجاهات
- [x] واجهة مستجيبة
- [ ] الرسوم البيانية المتقدمة (Charts)
- [ ] التصدير إلى PDF
- [ ] التقارير المخصصة

---

## 📚 المراجع

- [Flutter Documentation](https://flutter.dev/docs)
- [GetX State Management](https://github.com/jonataslaw/getx)
- [Material Design 3](https://m3.material.io/)

---

**تم التطوير بواسطة:** AI Assistant
**آخر تحديث:** يناير 2026
