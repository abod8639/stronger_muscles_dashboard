# 📱 لوحة تحكم Stronger Muscles Dashboard

لوحة تحكم شاملة لإدارة متجر إلكتروني متخصص في بيع مكملات بناء العضلات والمكملات الغذائية.

## ✨ الميزات الرئيسية

### 🏠 لوحة التحكم الرئيسية (Dashboard)
- عرض الإحصائيات الأساسية (الإيرادات، الطلبات، المستخدمين النشطين)
- رسوم بيانية للمبيعات اليومية
- قائمة أفضل 5 منتجات مبيعاً
- آخر 10 نشاطات على النظام

### 📦 إدارة المنتجات (Products)
- عرض قائمة المنتجات مع الفلاتر والبحث
- فلاتر حسب التصنيف والسعر والحالة
- عرض الصور والأسعار والخصومات
- حالة المخزون (متوفر/منخفض/منتهي)
- تقييمات المنتجات
- إضافة وتعديل وحذف المنتجات

### 🛒 إدارة الطلبات (Orders)
- عرض جميع الطلبات مع التفاصيل الكاملة
- فلاتر حسب حالة الطلب وحالة الدفع
- بحث برقم الطلب أو اسم العميل
- عرض تفاصيل الطلب والعناصر والملخص المالي
- تحديث حالة الطلب والدفع
- إلغاء الطلبات

### 👥 إدارة المستخدمين (Users)
- عرض قائمة المستخدمين
- فلاتر حسب الحالة (نشط/معطل)
- معلومات العميل (الطلبات، الإنفاق)
- تفعيل/تعطيل حسابات المستخدمين

## 📁 بنية المشروع

```
lib/
├── main.dart                    # نقطة دخول التطبيق
├── config/
│   └── theme.dart              # تكوين الألوان والنمط
├── models/
│   ├── user.dart               # نموذج المستخدم
│   ├── category.dart           # نموذج التصنيف
│   ├── product.dart            # نموذج المنتج
│   ├── order.dart              # نموذج الطلب والعناصر
│   ├── cart_item.dart          # نموذج عنصر السلة
│   ├── dashboard.dart          # نموذج بيانات لوحة التحكم
│   └── index.dart              # تصدير جميع النماذج
├── services/
│   ├── dashboard_service.dart  # خدمة جلب بيانات لوحة التحكم
│   ├── product_service.dart    # خدمة المنتجات
│   ├── order_service.dart      # خدمة الطلبات
│   ├── user_service.dart       # خدمة المستخدمين
│   ├── category_service.dart   # خدمة التصنيفات
│   └── index.dart              # تصدير جميع الخدمات
├── screens/
│   ├── dashboard_screen.dart   # شاشة لوحة التحكم
│   ├── products_screen.dart    # شاشة المنتجات
│   ├── orders_screen.dart      # شاشة الطلبات
│   ├── users_screen.dart       # شاشة المستخدمين
│   └── index.dart              # تصدير جميع الشاشات
└── components/
    ├── statistic_card.dart     # بطاقة الإحصائيات
    ├── section_header.dart     # رأس القسم
    ├── status_badge.dart       # شارات الحالة
    └── index.dart              # تصدير جميع المكونات
```

## 🎨 نظام الألوان

```dart
// الألوان الأساسية
Primary:     #1F77D4 (أزرق)
Secondary:   #FF6B6B (أحمر)
Success:     #2ECC71 (أخضر)
Warning:     #FFA500 (برتقالي)
Danger:      #E74C3C (أحمر داكن)
Info:        #3498DB (أزرق فاتح)
```

## 🌐 نقاط نهاية API

تم تكوين الخدمات للاتصال مع API بالعنوان الافتراضي:
```
Base URL: https://api.example.com
```

### الطلبات المتاحة:

#### لوحة التحكم
- `GET /api/dashboard` - جلب بيانات لوحة التحكم
- `GET /api/analytics/daily-sales?days=30` - بيانات المبيعات اليومية

#### المنتجات
- `GET /api/products` - جلب المنتجات (مع الفلاتر)
- `GET /api/products/{id}` - جلب منتج واحد
- `POST /api/products` - إضافة منتج
- `PUT /api/products/{id}` - تحديث منتج
- `DELETE /api/products/{id}` - حذف منتج

#### الطلبات
- `GET /api/orders` - جلب الطلبات (مع الفلاتر)
- `GET /api/orders/{id}` - جلب طلب واحد
- `PATCH /api/orders/{id}/status` - تحديث حالة الطلب
- `PATCH /api/orders/{id}/payment-status` - تحديث حالة الدفع
- `PATCH /api/orders/{id}/tracking` - تحديث رقم التتبع
- `POST /api/orders/{id}/cancel` - إلغاء الطلب

#### المستخدمين
- `GET /api/users` - جلب المستخدمين (مع الفلاتر)
- `GET /api/users/{id}` - جلب مستخدم واحد
- `PUT /api/users/{id}` - تحديث المستخدم
- `PATCH /api/users/{id}/status` - تغيير حالة المستخدم

#### التصنيفات
- `GET /api/categories` - جلب التصنيفات
- `GET /api/categories/{id}` - جلب تصنيف واحد
- `POST /api/categories` - إضافة تصنيف
- `PUT /api/categories/{id}` - تحديث تصنيف
- `DELETE /api/categories/{id}` - حذف تصنيف

## 📦 المكتبات المستخدمة

```yaml
dependencies:
  flutter: sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.0.0          # State Management
  fl_chart: ^0.68.0         # الرسوم البيانية
  google_fonts: ^6.0.0      # الخطوط
  intl: ^0.19.0             # التنسيقات الدولية
  http: ^1.1.0              # طلبات HTTP
  json_serializable: ^6.8.0 # تسلسل JSON

dev_dependencies:
  flutter_test: sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.0
```

## 🚀 البدء السريع

### 1. تثبيت المكتبات
```bash
cd stronger_muscles_dashboard
flutter pub get
```

### 2. تشغيل التطبيق
```bash
flutter run
```

### 3. بناء APK (Android)
```bash
flutter build apk --release
```

### 4. بناء IPA (iOS)
```bash
flutter build ios --release
```

## 📝 ملاحظات التطوير

### إضافة ميزة جديدة

1. **أنشئ نموذج البيانات** في `lib/models/`
2. **أنشئ الخدمة** في `lib/services/`
3. **أنشئ الشاشة** في `lib/screens/`
4. **أضف المسار** في `main.dart`

### معالجة الأخطاء

جميع الخدمات تتعامل مع الأخطاء باستخدام Try-Catch:

```dart
try {
  // عملية API
} catch (e) {
  throw Exception('خطأ: $e');
}
```

### تخزين البيانات

حالياً يتم تخزين البيانات في الذاكرة فقط. للتطوير المتقدم:
- استخدم `SharedPreferences` للبيانات البسيطة
- استخدم `Hive` أو `SQLite` للبيانات المعقدة
- استخدم `GetStorage` كبديل خفيف الوزن

## 🔒 الأمان

- تحقق من بيانات الإدخال قبل الإرسال
- استخدم HTTPS في الإنتاج
- احفظ التوكن بأمان
- لا تعرض بيانات حساسة في السجلات

## 📊 حالات الطلبات

```
pending (قيد الانتظار) → 🟡 أصفر
processing (قيد المعالجة) → 🔵 أزرق
shipped (تم الشحن) → 🟢 أخضر
delivered (تم التوصيل) → 🔷 فيروزي
cancelled (ملغى) → 🔴 أحمر
```

## 💡 نصائح للإنتاج

1. **تحسين الأداء**:
   - استخدم `const` في البناء
   - استخدم `ListView.builder` للقوائم الطويلة
   - اضغط الصور قبل الرفع

2. **التوافقية**:
   - اختبر على أجهزة مختلفة
   - اختبر الاتصالات السيئة
   - تعامل مع الأخطاء برشاقة

3. **الترجمة**:
   - استخدم حزمة `intl` للترجمة
   - اجعل النصوص في ملفات منفصلة

## 📞 الدعم والمساعدة

للمزيد من المعلومات:
- [Flutter Documentation](https://flutter.dev/docs)
- [Dart Documentation](https://dart.dev/guides)
- [fl_chart Documentation](https://github.com/imaNNeoFighT/fl_chart)

---

تم التطوير بواسطة: **Stronger Muscles Dashboard Team**
الإصدار: **1.0.0**
آخر تحديث: **January 2026**
