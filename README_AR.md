# 🏪 لوحة تحكم Stronger Muscles Dashboard

لوحة تحكم إدارية شاملة لمتجر إلكتروني متخصص في بيع مكملات بناء العضلات والمكملات الغذائية.

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![Flutter](https://img.shields.io/badge/Flutter-3.10+-brightgreen)
![Dart](https://img.shields.io/badge/Dart-3.0+-blue)
![License](https://img.shields.io/badge/license-MIT-green)

## 📋 المحتويات

- [الميزات](#-الميزات)
- [البنية](#-بنية-المشروع)
- [الاستخدام](#-البدء-السريع)
- [API](#-نقاط-نهاية-api)
- [المكتبات](#-المكتبات-المستخدمة)
- [الخصائص المتقدمة](#-الخصائص-المتقدمة)

---

## ✨ الميزات

### 🏠 لوحة التحكم الرئيسية
- **الإحصائيات الفورية**:
  - إجمالي الإيرادات من البداية
  - إيرادات اليوم الحالي
  - عدد الطلبات الجديدة والمعلقة
  - عدد المستخدمين النشطين
  - معدل التحويل

- **الرسوم البيانية**:
  - مخطط خطي لمبيعات آخر 30 يوم
  - مخطط دائري لتوزيع التصنيفات
  - مخطط أعمدة لأفضل المنتجات

- **البيانات المباشرة**:
  - أفضل 5 منتجات مبيعاً
  - آخر 10 نشاطات على النظام

### 📦 إدارة المنتجات

#### عرض وتصفية
- قائمة منتجات كاملة مع الصور
- فلاتر متقدمة:
  - البحث بالاسم أو الرقم
  - التصنيف
  - نطاق السعر
  - حالة المخزون
  - التقييم والمراجعات

#### المعلومات المعروضة
- **السعر**: السعر الأصلي والسعر بعد الخصم
- **الخصم**: نسبة الخصم المئوية
- **المخزون**: 
  - متوفر (أخضر)
  - منخفض (برتقالي)
  - منتهي (أحمر)
- **التقييم**: النجوم وعدد المراجعات

#### العمليات
- ✅ إضافة منتج جديد
- ✏️ تعديل المنتج
- 🗑️ حذف آمن مع تأكيد
- 📤 تصدير إلى Excel (قريباً)
- 📥 استيراد من ملفات (قريباً)

### 🛒 إدارة الطلبات

#### عرض وبحث
- قائمة جميع الطلبات
- بحث متقدم:
  - برقم الطلب
  - اسم العميل
  - البريد الإلكتروني
  - نطاق التاريخ

#### الفلاتر
- **حالة الطلب**: قيد الانتظار، قيد المعالجة، مشحون، تم التوصيل، ملغى
- **حالة الدفع**: معلق، مدفوع، فشل، مسترد

#### عرض التفاصيل
- معلومات العميل الكاملة
- قائمة العناصر المفصلة
- الملخص المالي:
  - المجموع الفرعي
  - تكلفة الشحن
  - الخصومات
  - الإجمالي النهائي
- تاريخ رقم التتبع

#### تحديث الحالة
- تغيير حالة الطلب
- تحديث حالة الدفع
- إضافة رقم التتبع
- إضافة ملاحظات
- إلغاء الطلب مع استرجاع الأموال

### 👥 إدارة المستخدمين

#### عرض والبحث
- قائمة المستخدمين
- البحث:
  - الاسم
  - البريد الإلكتروني
  - رقم الهاتف

#### الفلاتر
- الحالة (نشط/معطل)
- تاريخ الانضمام
- عدد الطلبات

#### معلومات العميل
- البيانات الشخصية
- عدد الطلبات
- إجمالي الإنفاق
- تاريخ الانضمام
- اللغة المفضلة

#### الإجراءات
- ✅ تفعيل/تعطيل الحساب
- 👁️ عرض التفاصيل
- 🔑 إعادة تعيين كلمة المرور
- ⚙️ تعديل الصلاحيات

---

## 📁 بنية المشروع

```
stronger_muscles_dashboard/
│
├── lib/
│   ├── main.dart                           # نقطة الدخول
│   │
│   ├── config/
│   │   └── theme.dart                      # النمط والألوان
│   │
│   ├── models/                             # نماذج البيانات
│   │   ├── user.dart                       # المستخدم
│   │   ├── category.dart                   # التصنيف
│   │   ├── product.dart                    # المنتج
│   │   ├── order.dart                      # الطلب والعناصر
│   │   ├── cart_item.dart                  # عنصر السلة
│   │   ├── dashboard.dart                  # بيانات لوحة التحكم
│   │   └── index.dart                      # تصدير
│   │
│   ├── services/                           # خدمات API
│   │   ├── dashboard_service.dart
│   │   ├── product_service.dart
│   │   ├── order_service.dart
│   │   ├── user_service.dart
│   │   ├── category_service.dart
│   │   └── index.dart                      # تصدير
│   │
│   ├── screens/                            # الشاشات
│   │   ├── dashboard_screen.dart
│   │   ├── products_screen.dart
│   │   ├── orders_screen.dart
│   │   ├── users_screen.dart
│   │   └── index.dart                      # تصدير
│   │
│   └── components/                         # المكونات المشتركة
│       ├── statistic_card.dart
│       ├── section_header.dart
│       ├── status_badge.dart
│       └── index.dart                      # تصدير
│
├── test/
│   └── widget_test.dart                    # اختبارات الويدجت
│
├── pubspec.yaml                            # المكتبات والإعدادات
├── README.md                               # هذا الملف
└── IMPLEMENTATION.md                       # التفاصيل التقنية
```

---

## 🚀 البدء السريع

### المتطلبات
- **Flutter**: 3.10 أو أحدث
- **Dart**: 3.0 أو أحدث
- **Android SDK**: 21 أو أحدث
- **Xcode**: 12.0 أو أحدث (لـ iOS)

### التثبيت

1. **استنساخ المشروع**
```bash
git clone https://github.com/your-repo/stronger_muscles_dashboard.git
cd stronger_muscles_dashboard
```

2. **تثبيت المكتبات**
```bash
flutter pub get
```

3. **تشغيل التطبيق**
```bash
flutter run
```

### الأوامر الشائعة

```bash
# تنظيف البناء
flutter clean

# الحصول على المكتبات
flutter pub get

# تشغيل على جهاز محدد
flutter run -d <device-id>

# بناء لـ Android
flutter build apk --release
flutter build appbundle --release

# بناء لـ iOS
flutter build ios --release

# بناء لـ Web
flutter build web --release

# اختبار
flutter test
```

---

## 🌐 نقاط نهاية API

### إعداد الخادم

غير `baseUrl` في الخدمات من:
```dart
static const String baseUrl = 'https://api.example.com';
```

إلى عنوان خادمك الفعلي.

### الطلبات المتاحة

#### 🏠 لوحة التحكم
```
GET  /api/dashboard                    # بيانات لوحة التحكم
GET  /api/analytics/daily-sales       # بيانات المبيعات اليومية
```

#### 📦 المنتجات
```
GET    /api/products                   # قائمة المنتجات
GET    /api/products/{id}              # تفاصيل منتج
POST   /api/products                   # إضافة منتج
PUT    /api/products/{id}              # تعديل منتج
DELETE /api/products/{id}              # حذف منتج
```

#### 🛒 الطلبات
```
GET    /api/orders                     # قائمة الطلبات
GET    /api/orders/{id}                # تفاصيل طلب
PATCH  /api/orders/{id}/status         # تحديث الحالة
PATCH  /api/orders/{id}/payment-status # تحديث حالة الدفع
PATCH  /api/orders/{id}/tracking       # تحديث التتبع
POST   /api/orders/{id}/cancel         # إلغاء الطلب
```

#### 👥 المستخدمين
```
GET    /api/users                      # قائمة المستخدمين
GET    /api/users/{id}                 # تفاصيل مستخدم
PUT    /api/users/{id}                 # تعديل المستخدم
PATCH  /api/users/{id}/status          # تغيير الحالة
```

#### 🏷️ التصنيفات
```
GET    /api/categories                 # قائمة التصنيفات
GET    /api/categories/{id}            # تفاصيل تصنيف
POST   /api/categories                 # إضافة تصنيف
PUT    /api/categories/{id}            # تعديل تصنيف
DELETE /api/categories/{id}            # حذف تصنيف
```

---

## 📦 المكتبات المستخدمة

| المكتبة | الإصدار | الغرض |
|--------|--------|-------|
| `flutter` | Latest | Framework الرئيسي |
| `provider` | ^6.0.0 | إدارة الحالة |
| `fl_chart` | ^0.68.0 | الرسوم البيانية |
| `google_fonts` | ^6.0.0 | الخطوط |
| `intl` | ^0.19.0 | الترجمة والتنسيق |
| `http` | ^1.1.0 | طلبات HTTP |
| `json_serializable` | ^6.8.0 | تسلسل JSON |
| `cupertino_icons` | ^1.0.8 | الأيقونات |

---

## 🎨 النمط والألوان

### نظام الألوان

```dart
📘 Primary (أزرق)        #1F77D4
🔴 Secondary (أحمر)       #FF6B6B
✅ Success (أخضر)        #2ECC71
⚠️ Warning (برتقالي)      #FFA500
❌ Danger (أحمر داكن)      #E74C3C
ℹ️ Info (أزرق فاتح)       #3498DB
```

### حالات الطلب

```
🟡 Pending (قيد الانتظار)    - برتقالي
🔵 Processing (قيد المعالجة) - أزرق
🟢 Shipped (مشحون)         - أخضر
🔷 Delivered (موصل)       - فيروزي
🔴 Cancelled (ملغى)        - أحمر
```

---

## 🔧 الخصائص المتقدمة

### إضافة نموذج جديد

1. **أنشئ الملف** في `lib/models/`
2. **اكتب النموذج** مع دعم JSON:

```dart
class MyModel {
  final int id;
  final String name;

  MyModel({required this.id, required this.name});

  factory MyModel.fromJson(Map<String, dynamic> json) {
    return MyModel(
      id: json['id'] as int,
      name: json['name'] as String,
    );
  }

  Map<String, dynamic> toJson() {
    return {'id': id, 'name': name};
  }
}
```

3. **أضفه إلى** `lib/models/index.dart`

### إضافة خدمة جديدة

```dart
import 'dart:convert';
import 'package:http/http.dart' as http;
import '../models/index.dart';

class MyService {
  static const String baseUrl = 'https://api.example.com';
  static const String endpoint = '/api/my-endpoint';

  static Future<List<MyModel>> getItems() async {
    try {
      final response = await http.get(
        Uri.parse('$baseUrl$endpoint'),
        headers: {'Content-Type': 'application/json'},
      ).timeout(const Duration(seconds: 10));

      if (response.statusCode == 200) {
        final json = jsonDecode(response.body);
        final List<dynamic> data = json['data'] ?? [];
        return data.map((item) => MyModel.fromJson(item)).toList();
      } else {
        throw Exception('فشل تحميل البيانات');
      }
    } catch (e) {
      throw Exception('خطأ: $e');
    }
  }
}
```

### إضافة شاشة جديدة

```dart
import 'package:flutter/material.dart';
import '../config/theme.dart';

class MyScreen extends StatefulWidget {
  const MyScreen({Key? key}) : super(key: key);

  @override
  State<MyScreen> createState() => _MyScreenState();
}

class _MyScreenState extends State<MyScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('عنوان الشاشة')),
      body: Container(
        // المحتوى
      ),
    );
  }
}
```

---

## 📱 الاستجابة والتوافقية

- **Android**: من 5.0 (API 21) وأعلى
- **iOS**: من 11.0 وأعلى
- **Web**: متوافق تماماً
- **Tablets**: تصميم متجاوب كامل
- **Dark Mode**: مدعوم

---

## 🧪 الاختبار

### تشغيل الاختبارات

```bash
# تشغيل جميع الاختبارات
flutter test

# تشغيل ملف اختبار محدد
flutter test test/widget_test.dart

# تشغيل مع التفاصيل
flutter test --verbose
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "فشل تحميل البيانات"

**الحل**:
1. تحقق من اتصال الإنترنت
2. تحقق من عنوان API الصحيح
3. تأكد من استجابة الخادم

### المشكلة: الصور لا تظهر

**الحل**:
1. تحقق من رابط الصورة
2. تأكد من صلاحية HTTPS
3. أضف معالج للأخطاء

### المشكلة: بطء التطبيق

**الحل**:
1. استخدم `const` في البناء
2. استخدم `ListView.builder` للقوائم الطويلة
3. اضغط الصور قبل الرفع

---

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:

1. Fork المشروع
2. أنشئ فرع جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للفرع (`git push origin feature/amazing-feature`)
5. Open Pull Request

---

## 📜 الترخيص

هذا المشروع مرخص تحت الرخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل.

---

## 📞 الاتصال والدعم

- 📧 البريد الإلكتروني: support@strongermuscles.com
- 🌐 الموقع: https://www.strongermuscles.com
- 💬 نتواصل: [@strongermuscles](https://twitter.com/strongermuscles)

---

## 🙏 شكر وتقدير

شكراً لاستخدام لوحة تحكم Stronger Muscles! 

---

**تم التطوير بواسطة**: Stronger Muscles Development Team  
**الإصدار**: 1.0.0  
**آخر تحديث**: January 4, 2026  
**الحالة**: ✅ نشط وقيد التطوير المستمر
