# 🎯 دليل الإعداد والاستخدام

## 📋 المتطلبات

- Flutter 3.10+
- Dart 3.10+
- خادم API يعمل على `localhost:8080` أو IP آخر
- اتصال إنترنت

---

## 🚀 الإعداد السريع

### 1. الإعداد الأولي

```bash
# تحديث الحزم
cd /home/dexter/flutter_apps/stronger_muscles_dashboard
flutter pub get

# تشغيل التطبيق
flutter run
```

### 2. اعدادات الاتصال بالخادم

الملف: `lib/config/api_config.dart`

**للتطوير المحلي:**
```dart
static const String baseUrl = 'http://localhost:8080/api/v1';
```

**للهاتف الحقيقي:**
```dart
static const String baseUrl = 'http://YOUR_IP_ADDRESS:8080/api/v1';
```

**لمحاكي Android:**
```dart
static const String baseUrl = 'http://10.0.2.2:8080/api/v1';
```

---

## 📊 البيانات المطلوبة من API

### 1. Endpoint: GET /api/v1/health
**الوصف:** التحقق من أن الخادم يعمل

**الاستجابة:**
```
200 OK
```

### 2. Endpoint: GET /api/v1/orders
**الوصف:** جلب جميع الطلبات

**الاستجابة:**
```json
[
  {
    "id": "string",
    "user_id": "string",
    "order_date": "ISO8601 datetime",
    "status": "pending|processing|shipped|delivered|cancelled",
    "payment_status": "pending|paid|failed|refunded",
    "payment_method": "string",
    "address_id": "string",
    "subtotal": "number",
    "shipping_cost": "number",
    "discount": "number",
    "total_amount": "number",
    "tracking_number": "string",
    "notes": "string",
    "items": []
  }
]
```

### 3. Endpoint: GET /api/v1/products
**الوصف:** جلب جميع المنتجات

**الاستجابة:**
```json
[
  {
    "id": "string",
    "name": "string",
    "price": "number",
    "discount_price": "number (optional)",
    "image_urls": ["string"],
    "description": "string",
    "category_id": "string",
    "stock_quantity": "integer",
    "average_rating": "number",
    "review_count": "integer",
    "brand": "string",
    "serving_size": "string",
    "servings_per_container": "integer",
    "is_active": "boolean"
  }
]
```

### 4. Endpoint: GET /api/v1/categories
**الوصف:** جلب جميع التصنيفات

**الاستجابة:**
```json
[
  {
    "id": "string",
    "name": "string",
    "description": "string",
    "image_url": "string",
    "sort_order": "integer",
    "is_active": "boolean"
  }
]
```

---

## 🎨 ميزات البيانات

### بيانات الطلبات:
- ✅ عرض الطلبات الأخيرة
- ✅ حالات الطلبات (معلقة، قيد المعالجة، مرسلة، مُسلّمة، ملغاة)
- ✅ حساب إجمالي الإيرادات
- ✅ عرض تفاصيل الدفع

### بيانات المنتجات:
- ✅ عرض المنتجات المتاحة
- ✅ حالة المخزون
  - المنتجات المتوفرة (> 10)
  - المخزون المنخفض (1-10)
  - غير متوفرة (0)

### بيانات التصنيفات:
- ✅ عرض التصنيفات في شبكة
- ✅ صور التصنيفات
- ✅ حالة التصنيف (نشط/معطل)

---

## 🔌 حالات الاتصال

### حالة 1: اتصال ناجح ✅
```
- الشاشة: عرض جميع البيانات
- AppBar: نقطة خضراء + "متصل"
- البيانات: محدثة من API
```

### حالة 2: فشل الاتصال ❌
```
- الشاشة: شاشة خطأ حمراء
- الرسالة: "لا يمكن الاتصال بقاعدة البيانات"
- زر: "إعادة محاولة"
- AppBar: نقطة حمراء + "غير متصل"
```

### حالة 3: عدم وجود بيانات ⚠️
```
- الشاشة: شاشة "لا توجد بيانات"
- الرسالة: "لم نتمكن من جلب أي بيانات"
- البيانات: قائمة فارغة
```

### حالة 4: مهلة الاتصال (Timeout) ⏱️
```
- المدة الافتراضية: 10 ثوان
- الخطأ: "Connection timeout"
- الحل: إعادة محاولة
```

---

## 🛠️ الدوال الرئيسية

### في DashboardController:

```dart
// جلب البيانات من API
Future<void> fetchDashboardData()

// إعادة محاولة الاتصال
Future<void> retryConnection()

// تغيير الفترة الزمنية
void changePeriod(String period)

// التحقق من حالة الاتصال
isConnected.obs  // RxBool

// رسالة الخطأ الحالية
errorMessage.obs  // RxString
```

---

## 📱 الملاحة

### الشاشات:

1. **لوحة التحكم** (Dashboard) - الشاشة الرئيسية
   - الإحصائيات الرئيسية
   - الطلبات الأخيرة
   - توزيع حالات الطلبات
   - التصنيفات
   - حالة المخزون

2. **الطلبات** (قريباً)
3. **المنتجات** (قريباً)
4. **المستخدمون** (قريباً)
5. **الإعدادات** (قريباً)

---

## 🧪 اختبار المحاكاة

### محاكي بسيط للخادم:

```dart
// في main.dart، يمكنك استخدام mock API
final bool useMockData = false;  // غير إلى true للاختبار
```

---

## ⚙️ خيارات الإعدادات

### في ApiService:

```dart
// مهلة الاتصال (ثانية)
static const int timeoutSeconds = 10;

// يمكنك تعديله:
static const int timeoutSeconds = 20;  // 20 ثانية
```

---

## 🐛 استكشاف الأخطاء

### الخطأ: "لا يمكن الاتصال"

**الحلول:**
1. تأكد من أن الخادم يعمل
2. تحقق من عنوان IP الصحيح
3. تأكد من الاتصال بالإنترنت
4. جرب `flutter clean && flutter pub get`

### الخطأ: "Timeout"

**الحلول:**
1. زيادة قيمة `timeoutSeconds`
2. تحقق من سرعة الإنترنت
3. تأكد من استجابة الخادم

### الخطأ: "Parse error"

**الحلول:**
1. تحقق من صيغة JSON من API
2. قارن مع نماذج البيانات
3. تحقق من السجلات في الـ console

---

## 📝 السجلات (Logs)

### تفعيل السجلات:

```dart
// في api_service.dart
print('خطأ في الاتصال: $e');  // تصحيح الأخطاء

// اضغط Ctrl+/ لفتح console
```

---

## 📚 الملفات المهمة

```
lib/
├── config/
│   └── api_config.dart          # اعدادات API
├── services/
│   └── api_service.dart         # خدمة الاتصال
├── repositories/
│   ├── order_repository.dart
│   ├── product_repository.dart
│   └── category_repository.dart
├── controllers/
│   └── dashboard_controller.dart # المتحكم الرئيسي
├── screens/
│   └── dashboard_screen.dart    # الشاشة الرئيسية
└── components/
    └── connection_status.dart   # مكونات حالة الاتصال
```

---

## 🎓 أمثلة الاستخدام

### جلب البيانات يدويّاً:

```dart
final controller = Get.find<DashboardController>();
await controller.fetchDashboardData();
```

### التحقق من الاتصال:

```dart
if (controller.isConnected.value) {
  // متصل
} else {
  // غير متصل
}
```

### الاستماع للتغييرات:

```dart
ever(controller.orders, (orders) {
  print('تم تحديث الطلبات: ${orders.length}');
});
```

---

## 🚀 الخطوات التالية

1. ✅ اختبر الاتصال مع خادمك
2. ✅ تحقق من استجابات API
3. ⏳ أضف authentication (Token)
4. ⏳ أضف caching للبيانات
5. ⏳ أضف تحديث تلقائي (polling)
6. ⏳ أضف واجهات أخرى

---

**آخر تحديث:** يناير 2026
**الإصدار:** 1.0.0
**الحالة:** جاهز للإنتاج 🎉
